const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
const motion=[...document.querySelectorAll('.motion-media')];const story=document.querySelector('.visual-story');const visuals=[...document.querySelectorAll('.visual')];if(visuals[0])visuals[0].classList.add('active');
let ticking=false;function render(){const y=scrollY;motion.forEach((el,i)=>{const r=el.parentElement.getBoundingClientRect();if(r.bottom>0&&r.top<innerHeight){const p=(innerHeight-r.top)/(innerHeight+r.height);const shift=(p-.5)*(i%2?42:58);el.style.transform=`scale(1.08) translate3d(0,${shift}px,0)`}});if(story&&visuals.length){const r=story.getBoundingClientRect();const progress=Math.max(0,Math.min(.999,-r.top/(r.height-innerHeight)));const idx=Math.min(visuals.length-1,Math.floor(progress*visuals.length));visuals.forEach((v,i)=>v.classList.toggle('active',i===idx))}ticking=false}window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(render);ticking=true}},{passive:true});render();