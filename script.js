const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('on')}),{threshold:.1});document.querySelectorAll('.reveal').forEach(x=>io.observe(x));
const motion=[...document.querySelectorAll('.motion-media')];const sports=document.querySelector('.sports-scroll');const track=document.querySelector('.sport-track');const progressBar=document.querySelector('.sport-progress i');let ticking=false;
function render(){motion.forEach((el,i)=>{const r=el.parentElement.getBoundingClientRect();if(r.bottom>0&&r.top<innerHeight){const p=(innerHeight-r.top)/(innerHeight+r.height);const shift=(p-.5)*(i%2?42:58);el.style.transform=`scale(1.08) translate3d(0,${shift}px,0)`}});if(sports&&track){const r=sports.getBoundingClientRect();const maxScroll=Math.max(0,track.scrollWidth-innerWidth);const range=Math.max(1,r.height-innerHeight);const p=Math.max(0,Math.min(1,-r.top/range));track.style.transform=`translate3d(${-maxScroll*p}px,0,0)`;if(progressBar)progressBar.style.width=`${p*100}%`}ticking=false}
window.addEventListener('scroll',()=>{if(!ticking){requestAnimationFrame(render);ticking=true}},{passive:true});window.addEventListener('resize',render);render();